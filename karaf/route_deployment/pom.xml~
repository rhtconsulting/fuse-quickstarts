<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.genfare.cloud</groupId>
    <artifactId>genfarecloud-parent</artifactId>
    <version>1.39.41</version>
  </parent>

  <artifactId>model</artifactId>
  <packaging>bundle</packaging>

  <name>model</name>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
    </dependency>

    <!-- Spring Data JPA -->
    <dependency>
      <groupId>org.springframework.data</groupId>
      <artifactId>spring-data-jpa</artifactId>
    </dependency>

    <!-- Hibernate -->
    <dependency>
      <groupId>org.hibernate</groupId>
      <artifactId>hibernate-core</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <plugin>
        <groupId>com.googlecode.java-formatter-maven-plugin</groupId>
        <artifactId>java-formatter-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <version>${version.maven.bundle.plugin}</version>
        <extensions>true</extensions>
        <configuration>
          <instructions>
            <Entities>,${classes;CONCRETE;ANNOTATION;javax.persistence.Entity}</Entities>
            <Export-Package>com.genfare.cloud.model.entity;version=${project.version},
              com.genfare.cloud.model.entity.view;version=${project.version},
              com.genfare.cloud.model.entity.inter;version=${project.version},
              com.genfare.cloud.model.listener;version=${project.version},
              com.genfare.cloud.model.repository;version=${project.version},
              com.genfare.cloud.model.user.provider;version=${project.version},</Export-Package>
            <Import-Package>org.apache.commons.lang.*,
              javax.persistence,
              javax.xml.bind.annotation,
              javassist.util.proxy,
              org.hibernate;version="[4.2,5)",
              org.hibernate.proxy;version="[4.2,5)",
              org.hibernate.annotations;version="[4.2,5)",
              org.springframework.data.*;version="[1.5,2)",
              org.slf4j</Import-Package>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <url>http://maven.apache.org</url>
</project>